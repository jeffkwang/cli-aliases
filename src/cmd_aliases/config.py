class Config:
    

    def read_aliases(self, filepath):
        """
        Reads aliases file.

        Returns:
            aliases (dict) - a Dictionary object mapping the aliases and their actual commands.
        """
        pass


config = Config()
